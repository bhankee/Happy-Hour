import React, { Component } from 'react';

const HappyPlace = ({ name }) => {
  return (
    <div>
      <div>{name}</div>
      <button>let's Go</button>
    </div>
  );
};

export default HappyPlace;
